#include <iostream> 
#include <cmath>
using namespace std;

// int getTerm();

int main() 
{
	int a, r, n, i;
	char choice;

	do {
		cout << "Geometric sequence calculator" << endl;
		cout << "\n\nInput value of first term (a): "; cin >> a;
		cout << "       Input common ratio (r): "; cin >> r;
		cout << "     Input number of term (n): "; cin >> n;
		cout << "\n\n  i               a*r^(i-1)" << endl;
		cout << "----------------------------------" << endl;
		cout << "      1              " << a << endl;
		cout << "      2              " << r << endl;
		cout << "      3              " << n << endl;
		
		cout << "\n\nSum of the terms (S): " << (a * (pow(r, n) - 1)) << endl;

		cout << "\nPress <any key> to try another valuer.\n";
			cout << "press <0> to exit the progeram.\n";
			cin >> choice;
			system("cls");
		

	} while (choice != '0'); {
		cout << "ABOUT THE PROGRAM\n\n";
		cout << "CPE 112/L - QUIZ 3\n";
		cout << "Developed by: James Ryne G. Velado\n";
		cout << "Course code: 9020\n";
		cout << "Station Number: 39\n";
	}

}
